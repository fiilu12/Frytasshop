[build]
builder = "nixpacks"
buildCommand = "cd ../.. && npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/api-server run build"

[deploy]
startCommand = "node --enable-source-maps /app/artifacts/api-server/dist/index.mjs"
restartPolicyType = "always"
